const reelStyle = theme =>  ({
  reel: {
    position: 'relative',
    width: '33%',
    minHeight: '100%',
    overflow: 'hidden',
    display: 'flex',
    flexDirection: 'column',
    justifyContent: 'flex-start',
    alignItems: 'center',
    border: '1px solid',
    zIndex: 100
  },
});

export default reelStyle;
